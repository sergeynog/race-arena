{
  "name": "race-coach",
  "private": true,
  "scripts": {
    "setup": "node scripts/generate-config.js",
    "test": "node --test tests/unit/merge-cloud-lap-shape.mjs",
    "test:e2e": "playwright test",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
